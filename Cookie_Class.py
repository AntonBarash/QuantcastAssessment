class Cookie():
    #cookie_list = []
    def __init__(self,cookie_name, cookie_date, cookie_time):
        self.name = cookie_name
        self.date = cookie_date
        self.time = cookie_time
        #self.cookie_list.append(self)

    #def add_cookie_to_cookie_list(cookie):
    #    self.cookie_list.append(cookie)

"""
c = Cookie('a','2018-09-09','04:04')
print(c.cookie_list)
b = Cookie('b','2018-09-09','04:04')
print(c.cookie_list)
print(b.cookie_list)"""
