Program which finds the most active cookie, with input in the command line being a filename of a csv log file of cookies, along with the date for the most active cookie to be found.

If multiple cookies meet that criteria, it returns all of them on separate lines.

Command to run python file from command line should look like this:

python .\most_active_cookie.py cookie_log.csv -d 2018-12-09
